{{!-- List of burgers ready to eat --}}
<div class="container" id="new-burgers">
    <div class="row">
        <div class="col-md-4">
            <ul class="list-group">
                {{#each burger}}
                    {{#unless devoured}}
                        {{> new-burger-block devoured=false}}
                    {{/unless}}
                {{/each}}
            </ul>
        </div>
    </div>
</div>

{{!-- User entry field to add burgers --}}
<div class="container">
    <div class="row">
        <div class="col-md-4">
            <div class="well">
                <form>
                    <div class="form-group">
                        <label for="new-burger">Prep a new burger</label>
                        <input type="text" class="form-control" id="new-burger-input" placeholder="What's the name of your burger?">
                    </div>
                    <button type="submit" class="btn btn-info">Submit</button>
                </form>
            </div>
        </div>
    </div>
</div>

{{!-- List of burgers that have been eaten --}}
<div class="container" id="devoured-burgers">
    <div class="row">
        <div class="col-md-4">
            <ul class="list-group">
                {{#each burger}}
                    {{#if devoured}}
                        {{> devoured-burger-block devoured=true}}
                    {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>
</div>